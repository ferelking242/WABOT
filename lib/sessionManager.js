'use strict';

/**
 * SessionManager — backward-compatible wrapper around SessionStateManager
 */

const { SessionStateManager } = require('./SessionStateManager');
module.exports = SessionStateManager;
